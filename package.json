{
  "name": "portfolio-site",
  "version": "1.0.0",
  "description": "Professional Engineer Portfolio Site",
  "scripts": {
    "sass": "sass styles/main.scss public/css/main.css --watch",
    "sass:build": "sass styles/main.scss public/css/main.css --style compressed",
    "build": "npm run sass:build",
    "dev": "sass styles/main.scss public/css/main.css --watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
